# Submission Notes

Project: GreenBin — Landing page (Next.js + Tailwind)

- Built with Next.js (app router) and Tailwind CSS.
- **Vercel v0 used to generate:** Hero section & Features cards (generated JSX then refined).
- Manually implemented: Testimonials, Contact form, Navbar, Footer, Login/Signup pages.
- Deployed to Vercel (see live link).
- To run locally: `npm install` → `npm run dev`. 
